# When a number is large, it'll become difficult to read. To make the long numbers more readable, you can group digits using underscores.

# count = 10_000_000_000

# print(count)

import math
print("Square of 4 is " + str(math.sqrt(4)))
print("2 raised to 4 is " + str(math.pow(2, 4)))
print("The maxmum of the numnbers is " + str(max(45, 6, 12, 1, 9)))
print("The natural logarithm of 2 is " + str(math.log(2)))
